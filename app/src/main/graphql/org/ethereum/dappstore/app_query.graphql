query AppQuery($id:ID!) {
    app(id: $id) {
        id
        appOwner
        appName
        appIPFSHash
        description
        developer
        type
        category
        logo
        version
        images
    }
}